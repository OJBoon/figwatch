{
  "name": "figwatch",
  "version": "1.0.0",
  "description": "AI-powered Figma comment watcher with macOS menu bar app. Comment @tone or @ux on any frame to get instant audits.",
  "author": "Olly Boon",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "watch": "node watcher/index.js",
    "build": "cd app && bash build.sh"
  },
  "optionalDependencies": {
    "figma-ds-cli": "^1.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
